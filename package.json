{
  "name": "plot-brew-design-qa",
  "version": "1.0.0",
  "private": true,
  "description": "Automated design QA scripts for Paged.js Plot Brew workbooks",
  "scripts": {
    "setup": "npm install && npx playwright install chromium",
    "serve": "node scripts/serve.js",
    "gapscan": "node scripts/gap-scan.js",
    "diffscan": "node scripts/diff-pages.js",
    "test": "node --test"
  },
  "devDependencies": {
    "pixelmatch": "^5.3.0",
    "playwright": "^1.51.0",
    "pngjs": "^7.0.0"
  }
}
